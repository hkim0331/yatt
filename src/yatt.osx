#!/usr/bin/env dash
#-*- mode: Makefile -*-
# VERSION: 0.17

RUBY=/usr/local/bin/ruby
YATT=${HOME}/bin/yatt.rb
LIB=${HOME}/Library/yatt
SERV=edu.melt.kyutech.ac.jp

${RUBY} ${YATT} --server ${SERV} --lib ${LIB}
