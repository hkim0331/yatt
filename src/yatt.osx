#!/usr/bin/env dash
#-*- mode: Makefile -*-
# VERSION: 0.12

RUBY=/usr/local/bin/ruby
YATT=/Users/hkim/bin/yatt.rb
LIB=/Users/hkim/Library/yatt
SERV=edu.melt.kyutech.ac.jp

${RUBY} ${YATT} --server ${SERV} --lib ${LIB}
