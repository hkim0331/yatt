#!/usr/bin/env dash
#-*- mode: shell-script -*-
# VERSION: 0.22.2

YATT=${HOME}/bin/yatt.rb
LIB=${HOME}/Library/yatt
SERV=web.melt.kyutech.ac.jp

/usr/bin/env ruby ${YATT} --server ${SERV} --lib ${LIB}
