#!/usr/bin/env dash
#-*- mode: shell-script -*-
# VERSION: 0.21.2

#RUBY=/usr/bin/env ruby
YATT=${HOME}/bin/yatt.rb
LIB=${HOME}/lib/yatt
SERV=edu.melt.kyutech.ac.jp

#${RUBY} ${YATT} --server ${SERV} --lib ${LIB}
ruby ${YATT} --server ${SERV} --lib ${LIB}
